import { createFileRoute } from '@tanstack/react-router'

import { DrHelioRussoPage } from '@/features/dr-helio-russo/DrHelioRussoPage'

export const Route = createFileRoute('/')({ component: DrHelioRussoPage })
